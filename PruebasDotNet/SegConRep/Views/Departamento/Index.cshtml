@model IEnumerable<SegConRep.departamento>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutMaster.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.departamento_nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.departamento_nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.departamento_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.departamento_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.departamento_id })
        </td>
    </tr>
}

</table>
