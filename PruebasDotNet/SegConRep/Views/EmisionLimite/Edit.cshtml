@model SegConRep.emisionlimite

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_LayoutMaster.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>emisionlimite</h4>
        <hr />
        @Html.ValidationSummary(true)
        <div class="form-group">
            @Html.LabelFor(model => model.emisionlimite_dias, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.emisionlimite_dias)
                @Html.ValidationMessageFor(model => model.emisionlimite_dias)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.emisionlimite_descripcion, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.emisionlimite_descripcion)
                @Html.ValidationMessageFor(model => model.emisionlimite_descripcion)
            </div>
        </div>

        @Html.HiddenFor(model => model.emisionlimite_producto_1)

        @Html.HiddenFor(model => model.emisionlimite_producto_2)

        @Html.HiddenFor(model => model.emisionlimite_proceso_id)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
