@model IEnumerable<SegConRep.perfil>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutMaster.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.perfil_nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.perfil_descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.perfil_nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.perfil_descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.perfil_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.perfil_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.perfil_id })
        </td>
    </tr>
}

</table>
