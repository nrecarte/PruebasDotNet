@model IEnumerable<SegConRep.emisionproceso>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutMaster.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.emisionproceso_nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.emisionproceso_descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.emisionproceso_nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.emisionproceso_descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.emisionproceso_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.emisionproceso_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.emisionproceso_id })
        </td>
    </tr>
}

</table>
