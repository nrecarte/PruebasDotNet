@model IEnumerable<SegConRep.perfilprivilegio>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutMaster.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.privilegio.privilegio_nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.perfil.perfil_nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.privilegio.privilegio_nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.perfil.perfil_nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.perfilprivilegio_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.perfilprivilegio_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.perfilprivilegio_id })
        </td>
    </tr>
}

</table>
