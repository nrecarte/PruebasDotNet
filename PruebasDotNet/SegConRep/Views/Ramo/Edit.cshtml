@model SegConRep.ramo

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_LayoutMaster.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>ramo</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.ramo_producto_1)

        @Html.HiddenFor(model => model.ramo_producto_2)

        <div class="form-group">
            @Html.LabelFor(model => model.ramo_nombre, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.ramo_nombre)
                @Html.ValidationMessageFor(model => model.ramo_nombre)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
