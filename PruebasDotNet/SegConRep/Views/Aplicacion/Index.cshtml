@model IEnumerable<SegConRep.aplicacion>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutMaster.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.aplicacion_nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.aplicacion_Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.aplicacion_nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.aplicacion_Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.aplicacion_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.aplicacion_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.aplicacion_id })
        </td>
    </tr>
}

</table>
