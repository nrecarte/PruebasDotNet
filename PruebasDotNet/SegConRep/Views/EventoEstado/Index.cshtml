@model IEnumerable<SegConRep.eventoestado>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutMaster.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.eventoestado_estado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.eventoestado_descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.eventoestado_estado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.eventoestado_descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.eventoestado_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.eventoestado_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.eventoestado_id })
        </td>
    </tr>
}

</table>
