@model IEnumerable<SegConRep.poliza>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutMaster.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.poliza_zona)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.poliza_oficina)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.polizaestado.polizaestado_estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.poliza_zona)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.poliza_oficina)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.polizaestado.polizaestado_estado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
